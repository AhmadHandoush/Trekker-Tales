import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ChatroomHeader = ({ user, parent }) => {
  return (
    <View>
      <Text>ChatroomHeader</Text>
    </View>
  );
};

export default ChatroomHeader;

const styles = StyleSheet.create({});
